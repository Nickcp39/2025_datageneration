# 说明：
# 1) 统一使用 PyTorch cu121 轮子，兼容 3060 / 4090 / 大工作站（前提：NVIDIA 驱动 >= 535）。
# 2) 第一行切到 PyTorch CUDA 轮子仓；第二行补回 PyPI 主仓给其它包用。
# 3) Python >= 3.10。

pip>=23; python_version >= "3.10"

--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

# ===== PyTorch 三件套（CUDA 12.1）=====
torch==2.4.1+cu121
torchvision==0.19.1+cu121
torchaudio==2.4.1+cu121

# ===== 训练/日志 & 工具 =====
tqdm>=4.66.0
tensorboard>=2.14.0
einops>=0.7.0
Pillow>=10.0.0
numpy>=1.24.0

# ===== 数据/图像处理（可选，但常用）=====
opencv-python
scikit-image
matplotlib
